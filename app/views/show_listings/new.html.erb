<%= form_for @showlisting do |f| %>

  <%= f.label :channel_id, "Channels" %>
  <%= f.collection_select(:channel_id, Channel.all, :id, :name, {}, {}) %>

  <%= f.label :show_id, "Shows" %>
  <%= f.collection_select(:show_id, Show.all, :id, :name, {}, {}) %>

  <%= f.label :date %>
  <%= f.text_field :date %>

  <%= f.submit "Create Show Listing" %>

<% end %>
